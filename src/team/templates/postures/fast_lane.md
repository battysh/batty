## Posture: Fast Lane
- Minimize exploration - go straight to the fix
- Skip exhaustive verification for low-risk changes (typos, config, docs)
- One commit, one completion - don't iterate if the fix is obvious
- Report immediately if the task is more complex than expected
